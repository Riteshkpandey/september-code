import math
for _ in range(int(input())):
    n=int(input())
    a=list(map(int,input().split()))
    c=0
    for i in range(n):
        if math.gcd(i+1,a[i])==a[i]:
            c+=1
    if c==n:
        print("YES")
    else:
        print("NO")
